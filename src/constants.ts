/** Default REST poll interval when env is unset. */
export const DEFAULT_POLL_INTERVAL_MS = 2000;

/** Human-readable service label for logs and help text. */
export const SERVICE_NAME = "kalshi-market-monitor";
